#.vim with Vundle

##Install:

    $ git clone --recursive https://bitbucket.org/dimsharav/vim_vundle ~/.vim
    $ cp ~/.vim/vimrc_example ~/.vimrc

Run **vim** and type 

    :BundleInstall

Just restart vim.
